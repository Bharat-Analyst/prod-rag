# Copy to .env and fill in.  .env is gitignored — never commit real keys.

# Required: answer generation with Gemini (rag_advanced.py, app.py).
# Get a key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Optional: raises Hugging Face download rate limits when first fetching
# the embedding and reranker models.
# HF_TOKEN=
